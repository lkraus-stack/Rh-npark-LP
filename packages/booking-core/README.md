# @franco/booking-core

Framework-agnostic booking logic for Franco Booking lives here. This package will hold
state machines, reducers, shared types, calculations, and validation helpers for both
`inquiry` and `transactional` flows.
