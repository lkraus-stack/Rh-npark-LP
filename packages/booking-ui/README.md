# @franco/booking-ui

Shared React booking UI primitives live here, including drawer and wizard chrome,
forms, segment selectors, date pickers, pricing surfaces, and motion primitives. The
package owns reusable UI structure only; customer apps provide CI through theme tokens.
